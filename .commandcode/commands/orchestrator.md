Read the file `skills/orchestrator/SKILL.md` in this repository and follow it exactly, from
step 1. Read `RULES.md` at the repo root first — its hard rules bind and they are not
defaults to optimise around.

Do not summarise the skill, skip its verification steps, or substitute your own approach
for its flow.

Task context from the user: $ARGUMENTS
