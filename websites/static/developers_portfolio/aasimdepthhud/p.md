> **Reconstructed prompt.** This template arrived as HTML with no prompt. This file was
> written by reading the markup — it describes what the page *is*, not what anyone asked
> for. It is not evidence of intent.

"Design a full-stack developer portfolio that reads as a descent from surface product work to systems internals.

Zoho automation, CRM sync, AI extraction pipelines, a Rust interpreter. Depth increases down the page: brass accents up top fade toward teal at the bottom.

Visual: dark navy (#0A0E16) with brass (#C9A227) fading to teal (#6FC8BC), Bricolage Grotesque display + Manrope body + IBM Plex Mono data readouts, Tailwind utilities for layout only.

Page Structure:
Chrome: no standard nav — a fixed HUD readout top-left (name + live depth in metres) and a right-edge scrollspy rail replace it.
Hero Section: name pinned bottom-left at huge scale; a floating HUD 'status' panel top-right (depth/role/base/status); bracket-style mono links, not pills.
About: two-column text plus a mono readout stat list (years/projects/languages/compiler).
Skills: a vertical 'core sample' — one gradient bar with four category blocks branching off it (Surface/Application/Intelligence/Systems), each tagged with a depth.
Work: a click-to-expand log/manifest (native details elements) — Fieldwork, Compass CRM Sync, Ledger, Strata — each with an inline SVG visual.
Experience: a self-drawing dive-profile line chart (solid career path, dashed self-study branch) feeding a manifest-style log.
Contact: quiet sign-off reusing the readout-line component (mail/code/profile).

Interaction Details:
- The depth readout counts 0000–6200 m with scroll position; the rail dots highlight the section in view.
- An ambient particle field drifts behind the page; the camera creeps forward on scroll.
- Hero entrance is one orchestrated sequence; skill blocks parallax at staggered rates; the dive path draws itself; stats count up once.
- Project entries expand with an animated grid-rows transition and rotating chevron; HUD links have a magnetic pull.
- Reduced motion is respected throughout (static canvas, final states shown instantly).

Overall Vibe: instrument-like, deep-sea, mono-data Scientist meets product engineer."
