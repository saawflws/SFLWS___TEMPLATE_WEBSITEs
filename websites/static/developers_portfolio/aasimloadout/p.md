> **Reconstructed prompt.** This template arrived as HTML with no prompt. This file was
> written by reading the markup — it describes what the page *is*, not what anyone asked
> for. It is not evidence of intent.

"Design a full-stack developer portfolio that plays like an expedition loadout screen over a voxel landscape.

RAG systems, Zoho automation, a Rust bytecode VM, Go chunk storage, ML drift monitoring. Each project opens a 'field report' with a generative seed-topography map.

Visual: warm near-black (#0D0C0A) with bone text (#EAE3D6) and ember accents (#E8A33D), light Fraunces serif with italic ember highlights + JetBrains Mono labels, Tailwind utilities.

Page Structure:
Chrome: fixed nav (numbered links + availability pill + mobile menu), bottom-left HUD (Y coordinate, biome readout, scroll gauge), bottom-right seed tag.
Hero Section: typewriter eyebrow, huge serif headline ('building intelligent systems from browser to bedrock'), dual CTAs, over a full-bleed draggable 3D voxel terrain with torch light and rising embers.
Ticker: an infinite skill marquee ribbon dividing hero from content.
About: sticky ID card (blinking pixel avatar, name/class/base/status, level/coords stats, advancements) beside narrative plus a count-up ledger.
Skills: a 9-slot hotbar (rarity tiers, hover tooltips) driving a loadout detail panel (lore, enchantments, proficiency bar, years/deployed/last-used), plus a full searchable inventory overlay on [E].
Projects: five field-report rows (Cartograph, Waypoint Ops, oxvm, chunkd, driftwatch) with generative pixel-topography thumbnails and complexity blocks.
Experience: a mineshaft descent (Y 128 to bedrock) with alternating strata cards on a scroll-filling shaft.
Contact: direct email channel plus a validated message form that composes via the visitor's mail app.

Interaction Details:
- Preloader runs 'generating world' percent with cycling status messages, then wipes into a choreographed hero intro.
- The voxel terrain is deterministic seeded noise (stepped columns, dusted peaks, ember ore, torch flicker); drag rotates, scroll dollies the camera and thickens the fog.
- The HUD tracks depth (Y 64 to −64) and scrambles the biome name per section; nav links highlight by scroll position.
- Hotbar slots inspect on hover and select a full detail readout; the inventory overlay filters 40+ items live.
- Project thumbnails glitch-scramble on hover; modals pair the write-up with a large seeded topography canvas.
- The contact form validates inline (error borders + shake + toast) and falls back to mailto — no backend.
- Custom dot-and-ring cursor (DRAG badge over the hero, hidden over inputs); magnetic buttons; Lenis smooth scroll.
- Reduced motion is respected; without GSAP the loader is removed and the page renders statically.

Overall Vibe: warm-dark, expedition-gamer, inventory-proud, systems-deep."
