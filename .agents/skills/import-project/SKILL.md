---
name: import-project
description: Import a full React, Next.js or Astro project from raw_make_websites/ into the websites/ shelf, excluding node_modules, with copy verification and a never-auto-delete source flag.
---

# import-project — pointer

The real instructions live in [`skills/import-project/SKILL.md`](../../../skills/import-project/SKILL.md).

Read that file now and follow it exactly. This file is a discovery stub so the
skill is findable at this path; it holds no logic of its own and must never be
given any (see `RULES.md` rule 7 — skill logic lives in one place).

Read [`RULES.md`](../../../RULES.md) before acting.
