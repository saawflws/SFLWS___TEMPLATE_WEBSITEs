---
name: ingest-template
description: Catalog a single static website template (a p.md + index.html pair) into websites/static/, verify its category from the markup, write META.md, update INDEX.md, and regenerate data.js.
---

# ingest-template — pointer

The real instructions live in [`skills/ingest-template/SKILL.md`](../../../skills/ingest-template/SKILL.md).

Read that file now and follow it exactly. This file is a discovery stub so the
skill is findable at this path; it holds no logic of its own and must never be
given any (see `RULES.md` rule 7 — skill logic lives in one place).

Read [`RULES.md`](../../../RULES.md) before acting.
