---
name: generate-site
description: Build a new website from the SFLWS template shelf via the public-agents entry chain, choosing a template and a strict-clone or loose-reference mode.
---

# generate-site — pointer

The real instructions live in [`skills/generate-site/SKILL.md`](../../../skills/generate-site/SKILL.md).

Read that file now and follow it exactly. This file is a discovery stub so the
skill is findable at this path; it holds no logic of its own and must never be
given any (see `RULES.md` rule 7 — skill logic lives in one place).

Read [`RULES.md`](../../../RULES.md) before acting.
