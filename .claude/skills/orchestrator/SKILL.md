---
name: orchestrator
description: Route an SFLWS task to the right skill: cataloging a static template, importing a framework project, or generating a new site from the shelf. Use when it is not obvious which applies.
---

# orchestrator — pointer

The real instructions live in [`skills/orchestrator/SKILL.md`](../../../skills/orchestrator/SKILL.md).

Read that file now and follow it exactly. This file is a discovery stub so the
skill is findable at this path; it holds no logic of its own and must never be
given any (see `RULES.md` rule 7 — skill logic lives in one place).

Read [`RULES.md`](../../../RULES.md) before acting.
